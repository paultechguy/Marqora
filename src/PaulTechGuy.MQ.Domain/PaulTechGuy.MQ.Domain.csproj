<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Pure model layer: enums, records and value objects.
    Deliberately has zero package or project references so every other layer,
    and every future test project, can depend on it freely.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>PaulTechGuy.MQ.Domain</RootNamespace>
  </PropertyGroup>

</Project>
